openapi: '3.1.0'
info:
  title: 'Task API'
  version: '1.0.0'
servers:
  - url: 'http://localhost:8080'  
tags:
  - name: 'Tasks'
    description: 'Operations related to tasks'
paths:
  /tasks:
    $ref: 'paths/tasks.yaml'
